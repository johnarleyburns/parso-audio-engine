/* Placeholder module for vendored libFLAC (BSD-3-Clause).
 * Replace this target's contents with the upstream libFLAC source tree
 * (native-FLAC only; do NOT compile Ogg sources — no libogg dependency).
 * Keep only C-clean headers in include/. See docs/SPEC.md §4. */
#ifndef PARSO_CFLAC_SHIM_H
#define PARSO_CFLAC_SHIM_H
/* Intentionally empty until libFLAC is vendored. */
#endif
