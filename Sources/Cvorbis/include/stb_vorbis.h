#ifndef PARSO_STB_VORBIS_H
#define PARSO_STB_VORBIS_H

/* stb_vorbis is distributed as a single .c file. Its supported header-only
 * mode exposes declarations here while src/stb_vorbis_impl.c emits the code. */
#define STB_VORBIS_HEADER_ONLY
#include "stb_vorbis.inc"

#endif
