# Vendoring Signalsmith Stretch (MIT)
Copy `signalsmith-stretch.h` and its dependency headers (signalsmith-linear / DSP) here.
Enable Accelerate FFT via the `SIGNALSMITH_USE_ACCELERATE` define (already set in Package.swift).
Keep upstream LICENSE. https://github.com/Signalsmith-Audio/signalsmith-stretch
Source commit: <fill in>
